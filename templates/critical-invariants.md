# Critical Invariants

These rules MUST be followed in every response. They are re-injected after /compact.

## ALWAYS

- Run tests before claiming code works
- Use the project's package manager (check lockfile)
- Follow existing code patterns and conventions

## NEVER

- Use `--no-verify` on git commits
- Write secrets to files (use environment variables)
- Skip error handling in new code

---
*Keep this file under 20 lines. It's injected into every session.*
